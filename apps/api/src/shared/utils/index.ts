/**
 * Shared utility functions.
 *
 * Add project-wide helper functions here — utilities that are used by
 * multiple modules and do not belong to any single feature.
 *
 * Examples:
 *   export function paginate(page: number, limit: number) { ... }
 *   export function slugify(text: string): string { ... }
 *   export function toISODate(date: Date): string { ... }
 */

// TODO: populate as modules are implemented
