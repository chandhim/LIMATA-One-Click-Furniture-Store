/**
 * Placement module — placeholder for future implementation.
 *
 * Planned functionality:
 *   AR/3D furniture placement — integrates with model3dUrl field on Product.
 *
 * Planned endpoints:
 *   GET    /api/placement/session  — get placement session config
 *   POST   /api/placement/session  — create placement session
 */

// TODO: implement placement.route.ts, placement.controller.ts,
//       placement.service.ts, placement.repository.ts, placement.types.ts
